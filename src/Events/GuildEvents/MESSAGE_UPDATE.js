class MESSAGE_UPDATE {
  constructor(client) {
    this.client = client;
  }

  /**
   *
   * @param {Object} packet The packet of the event.
   */
  async handle(packet) {
    this.client.eventHandler.emitEvent("MessageUpdated", packet);
  }
}

module.exports = MESSAGE_UPDATE;
